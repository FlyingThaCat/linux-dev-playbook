- name: Install Temurin (Adoptium) on Archlinux
  become: false
  ansible.builtin.command: yay -S --noconfirm jdk{{ java_major_version }}-temurin
  args:
    creates: /usr/bin/java
  when: ansible_facts.os_family == "Archlinux"
