---
- name: Test pnpm installation
  hosts: localhost
  gather_facts: true
  connection: local

  tasks:
    - name: Include pnpm tasks
      ansible.builtin.include_tasks: ../../tasks/nodejs/pnpm.yml
